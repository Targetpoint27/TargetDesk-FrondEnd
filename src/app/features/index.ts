import { Routes } from '@angular/router';

// Feature routes configuration - à implémenter
export const FEATURE_ROUTES: Routes = [
  // Routes will be implemented later when feature modules are created
];

export default FEATURE_ROUTES;